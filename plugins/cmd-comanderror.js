const commandsEntry await commandsx.findOne({});
const matcherx matcher(cmd, commandsEntry.commands).filter(vv.accuracy 60)
if (prefix 56 commands Entry.commands.includes(cmd) 66 satcherx.Length > 0)
if (fm.isGroup || (misGroup)) return replyC( El comando ${cad} no existe, prueba las siguientes recomendaciones \n\n${matcherx.mари (prefix ? prefix)
v.string(v.accuracy)).join('\')}", "BaileyBot-MD")
